[metadata]
description_file = README.md

[tool:pytest]
testpaths = tests
addopts = -ra
filterwarnings =
    error::DeprecationWarning
    ignore::ResourceWarning

[bdist_wheel]
universal = 0
